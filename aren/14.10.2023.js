function calc(a, sign) {
 function doll() {
    if (5===5) return true;
 } return doll();
}
calc(1, '+');
function customGreet(name, message) {
    return function doll() {
        console.log(`${message} ${name}`);
    }
}
let hiAra = customGreet('Ara', 'Hi');

function calc(first, sign) {
    return function big() {
        if (sign === '+');
         return sum10
    }
}

let sum10 = calc(10, '+');

sum10(20) // 30

calc(1)('+')(2);
function call() {
    return function b(sign) {
        return function c(second) {
            switch(sign) {
                case '+': return first + second;
                case '-': return first - second;
                case '*': return first * second;
                case '/': return first / second;
            }
        }
    }
}